# Commit Deep Dive - 2026-03-21 - 7a88706

## Commit
- Full SHA: `7a88706374fcaaed9639a949699cef1967ed0422`
- Short SHA: `7a88706`
- Subject: `chore: commit-deep-dive와 write-commit-message 스킬 추가`
- Date: `2026-03-21`

## What changed
- `commit-deep-dive` 스킬을 추가해 특정 커밋의 변경점, 학습 포인트, 인터뷰용 설명을 문서로 남길 수 있게 했다.
- `collect_commit_context.py` 스크립트를 추가해 커밋 메타데이터, 변경 파일, diff stat, patch excerpt를 구조화해서 수집하게 했다.
- `write-commit-message` 스킬과 `collect_commit_message_context.py` 스크립트를 추가해 staged/working tree/revision 기준으로 커밋 메시지를 추천할 수 있게 했다.
- `docs/learning/2026-03-20-7b34d6c.md`를 생성해 실제 deep-dive 산출물 예시를 저장소에 남겼다.
- 즉, 단순히 기능을 만들기보다 “내가 만든 작업을 다시 학습 자산으로 남기는 루프”를 도구로 만들었다.

## Why it matters
- 이 커밋은 ThinkWrite 제품 코드 자체보다, 개발 과정에서 지식을 회수하는 워크플로우를 자동화한 변화다.
- 커밋을 학습 노트와 커밋 메시지 추천으로 다시 해석하게 만들면, 구현이 끝난 뒤 맥락을 잊어도 히스토리를 더 잘 재사용할 수 있다.
- 특히 오픈소스나 포트폴리오 관점에서 “왜 이렇게 바꿨는지”를 후속 문서로 남기는 습관은 인터뷰와 회고 품질을 높여준다.

## Key files
- `.codex/skills/commit-deep-dive/SKILL.md`: deep-dive 문서가 어떤 구조와 품질 기준을 따라야 하는지 정의한다.
- `.codex/skills/commit-deep-dive/scripts/collect_commit_context.py`: 커밋 분석 전에 어떤 정보를 구조화해서 보는지 이해하기 좋다.
- `.codex/skills/write-commit-message/SKILL.md`: diff 기반 커밋 메시지 추천 워크플로우를 문서화한 파일이다.
- `.codex/skills/write-commit-message/scripts/collect_commit_message_context.py`: staged/working tree/rev를 자동 판별해 메시지 추천의 근거를 만든다.
- `docs/learning/2026-03-20-7b34d6c.md`: deep-dive 스킬의 실제 산출물 예시로 참고할 수 있다.

## Concepts to study
- Git diff를 구조화해서 해석하는 방법
- 커밋 히스토리를 학습 아티팩트로 전환하는 워크플로우
- staged diff vs working tree diff의 차이
- 스킬 문서와 보조 스크립트의 역할 분리
- 회고/인터뷰용 문서 자동화 설계

## Deep dive questions
- 왜 단순한 diff 요약이 아니라 “학습 노트” 형식으로 남기는 것이 더 유용할까?
- commit-deep-dive와 write-commit-message는 각각 어떤 문제를 줄여주고, 함께 쓸 때 어떤 시너지가 있을까?
- 스킬 문서와 수집 스크립트를 분리하는 구조는 어떤 유지보수 장점을 만들까?
- staged-first, working-tree fallback 전략은 커밋 메시지 품질에 어떤 영향을 줄까?
- 구현 커밋마다 학습 노트를 남기는 습관은 언제는 가치가 크고, 언제는 오히려 과할까?

## Interview framing
- 기능 구현뿐 아니라 커밋을 학습 자산으로 재사용하는 도구를 함께 만들어 개발 회고 비용을 줄였다.
- diff 해석 로직은 스크립트로 구조화하고, 결과물 포맷은 스킬 문서로 분리해 재사용성과 수정 편의성을 높였다.
- 실제 예시 노트까지 함께 남겨 이후 커밋에도 같은 품질 기준을 반복 적용할 수 있게 했다.

## Next study actions
### 15 min
- `collect_commit_context.py`와 `collect_commit_message_context.py`가 각각 어떤 정보를 수집하는지 비교표를 만든다.

### 30 min
- 현재 저장소에서 최근 커밋 2개를 골라 deep-dive 없이 스스로 요약한 뒤, 스킬 산출물과 비교한다.

### 60 min
- “커밋 → 학습 노트 → 인터뷰 포인트”로 이어지는 개인 개발 루프를 일반화해서 다른 프로젝트에도 적용 가능한 템플릿으로 정리한다.
