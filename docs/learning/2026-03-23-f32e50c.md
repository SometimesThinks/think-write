# Commit Deep Dive - 2026-03-23 - f32e50c

## Commit
- Full SHA: `f32e50c22d7a6d8d85e5838826b1e4c8d5b0134f`
- Short SHA: `f32e50c`
- Subject: `chore: 자동완성 공용 타입 추가 및 MVP 체크리스트 정리`
- Date: `2026-03-23`

## What changed
- 자동완성 요청/응답에 공통으로 쓸 `AutocompleteRequest`, `AutocompleteResponse`, `LanguageSettings` 타입을 `src/shared/autocomplete.ts`에 추가했다.
- `prefix`와 `suffix`의 1차 실험 길이 제한을 상수로 함께 정의해 이후 서버/클라이언트가 같은 기준을 참조할 수 있게 했다.
- MVP 체크리스트의 Step 0을 구체화해 요청 필드, 응답 필드, stateless 원칙, 응답 최소 스펙을 문서로 확정했다.
- 기존 체크리스트 순서를 백엔드 계약과 서버 골격을 먼저 만드는 흐름으로 재배치했다.
- Ghost Complete 요청 트리거 항목에서 더 이상 쓰지 않기로 한 `paragraphContext`를 제거해 문서와 구현 방향을 맞췄다.

## Why it matters
- 이 커밋은 Ghost Complete를 구현하기 전에 “무엇을 주고받을지”를 먼저 고정한 작업이라 이후 구현 흔들림을 줄인다.
- 공용 타입 파일을 먼저 만들면 클라이언트와 서버가 서로 다른 요청 형태를 상상하며 개발하는 문제를 막을 수 있다.
- 체크리스트 순서를 백엔드 계약 우선으로 바꾼 것은 실제 작업 흐름을 더 현실적으로 만든 결정이다.
- 아직 기능을 직접 추가한 커밋은 아니지만, 이후 `/api/completion`, OpenAI adapter, editor 연동을 더 작고 명확한 단위로 진행할 기반을 만들었다.

## Key files
- `src/shared/autocomplete.ts`: 자동완성 API의 최소 계약과 길이 제한 상수를 한 파일에 모아 공용 참조 지점을 만들었다.
- `docs/plan/MVP-세부-작업-체크리스트.md`: 요청/응답 스펙을 구체화하고 실제 구현 순서를 백엔드 우선 흐름으로 다시 정렬했다.

## Concepts to study
- API contract-first 설계
- 클라이언트/서버 공용 TypeScript 타입 배치 전략
- stateless autocomplete request 설계
- prefix/suffix 기반 문맥 추출 전략
- 길이 제한 상수를 통한 prompt budget 관리
- 계획 문서와 실제 구현 순서 정렬 방식

## Deep dive questions
- 자동완성 기능에서 `currentParagraph` 없이 `prefix`와 `suffix`만으로 시작할 때의 장점과 한계는 무엇일까?
- 공용 타입 파일을 `src/shared/`에 두는 방식은 언제 적절하고, 언제 더 세분화된 구조가 필요할까?
- 요청/응답 계약을 먼저 고정하는 방식은 초기 개발 속도를 늦추는 대신 어떤 재작업을 줄여줄까?
- 성공 응답에서 `provider`나 `model`을 제외하는 판단은 어떤 시점에 다시 뒤집힐 수 있을까?
- 체크리스트를 기능 분류 순서가 아니라 실행 순서로 정렬할 때 얻는 이점과 잃는 점은 무엇일까?

## Interview framing
- Ghost Complete 구현 전에 요청/응답 계약을 먼저 확정해 이후 프론트엔드와 백엔드가 같은 타입을 기준으로 움직이게 했다.
- 초기 MVP에서는 문맥을 단순하게 유지하기 위해 `prefix`/`suffix` 중심의 stateless 요청으로 시작하고, 품질 부족 시에만 맥락 필드를 확장하도록 결정했다.
- 문서상 기능 목록보다 실제 개발 순서를 더 중요하게 보고, 체크리스트를 백엔드 계약 → 저장 계층 → adapter → UI 순서로 재정렬했다.

## Next study actions
### 15 min
- `src/shared/autocomplete.ts`를 기준으로 `/api/completion` 요청 바디 예시와 에러 응답 예시를 짧게 적어본다.

### 30 min
- `prefix` 1200 / `suffix` 300 제한이 실제로 몇 토큰 정도가 될지 대략 계산하고, 비용과 품질 관점에서 조정 후보를 적어본다.

### 60 min
- stateless autocomplete 설계와 세션 누적 방식의 trade-off를 정리하고, ThinkWrite MVP에서 왜 stateless로 시작하는지 근거를 문서로 남긴다.
