# Commit Deep Dive - 2026-03-22 - e4a221e

## Commit
- Full SHA: `e4a221e9478dc2dd41231fa2d8a3a47919126568`
- Short SHA: `e4a221e`
- Subject: `docs: MVP 의사결정기록 추가 및 기존 문서 정합화`
- Date: `2026-03-22`

## What changed
- `docs/technical/MVP-의사결정기록.md`를 추가해 OpenAI + `gpt-5-nano`, API Key 서버 영구 저장, ghost 오류 노출 정책을 한 문서에 정리했다.
- 기존 product/technical 문서를 수정해 새 결정이 기획, 요구사항, 기능 정의, 아키텍처, API, ADR에 반영되도록 정합화를 맞췄다.
- BYOK 정책을 브라우저 저장 가정에서 서버 영구 저장 + 암호화 + 원문 비재노출 기준으로 명확히 바꿨다.
- 초기 MVP provider/model을 OpenAI + `gpt-5-nano`로 고정하고, 관련 API 예시와 기술 스택 문서도 함께 수정했다.
- ghost 정상 흐름은 editor 내부 ghost text, 전역 설정 문제는 toast, ghost 흐름 문제는 inline/무표시로 분리하는 UX 기준을 문서에 심었다.

## Why it matters
- 이 커밋은 흩어진 결정을 한 번에 모아 “이제 구현할 때 더 이상 추측하지 않아도 되는 상태”를 만든다.
- 특히 BYOK 저장 정책과 오류 노출 방식은 UI/API/보안 경계 모두에 영향을 주는 의사결정이라, 문서 정합화가 안 되면 구현 중 재작업 가능성이 높다.
- 제품 문서와 기술 문서가 같은 방향을 보게 정리했기 때문에 이후 구현 체크리스트와 코드 설계가 한층 안정된다.

## Key files
- `docs/technical/MVP-의사결정기록.md`: 이번에 확정한 MVP 수준 결정과 장단점, 반대급부를 한 번에 볼 수 있다.
- `docs/product/기획서.md`: 상위 제품 문서에 초기 구현 결정을 어디까지 넣을지 살펴볼 수 있다.
- `docs/product/요구사항-정의서.md`: UX/기능 요구사항에 서버 저장과 오류 노출 정책이 어떻게 번역됐는지 볼 수 있다.
- `docs/technical/API-명세서.md`: 기본 provider/model, key API, 에러 처리 원칙이 구체 API 레벨로 어떻게 연결되는지 확인할 수 있다.
- `docs/technical/백엔드-의사결정기록.md`: 영구 저장 정책과 초기 provider 고정을 ADR 수준으로 남긴 예시다.

## Concepts to study
- 문서 정합화(workstream alignment)
- BYOK 영구 저장 보안 경계
- 제품 요구사항과 기술 ADR의 연결 방식
- editor-context error UX 설계
- 기본 provider/model 고정의 장단점
- 문서 주도 구현 준비 방식

## Deep dive questions
- 왜 BYOK 저장 정책 하나가 product docs, API docs, architecture docs를 동시에 바꾸게 만들까?
- 기술 결정이 문서 여러 군데에 반복 반영될 때 어떤 방식으로 정합성을 유지하는 것이 좋을까?
- 초기 provider/model을 고정하는 결정은 구현 속도를 얼마나 높이고, 나중에 어떤 비용을 남길까?
- ghost text UX에서 toast와 inline notice를 분리하는 기준은 왜 중요한가?
- 제품 의사결정 기록을 별도 문서로 두는 것이 ADR만 사용하는 것과 어떻게 다를까?

## Interview framing
- 구현 전에 흩어진 결정을 한 문서로 모으고 기존 문서 전반에 반영해, 이후 개발이 문서 기준으로 일관되게 진행되도록 정리했다.
- BYOK 저장 정책, 기본 모델, 오류 UX처럼 여러 계층에 영향을 주는 결정을 먼저 고정해 재작업 리스크를 줄였다.
- 제품 문서와 기술 문서의 정합성을 맞추는 작업도 구현 준비의 일부로 보고 처리했다.

## Next study actions
### 15 min
- 이번에 수정된 문서들을 나란히 놓고 동일한 결정이 각 문서에서 어떤 표현으로 번역됐는지 표시한다.

### 30 min
- BYOK 서버 영구 저장 정책을 실제 구현할 때 필요한 암호화/삭제/조회 상태 요구사항을 별도 메모로 뽑는다.

### 60 min
- 문서 정합화가 안 된 상태에서 구현을 시작했을 때 생길 수 있는 재작업 시나리오를 3개 이상 써보고 대비책을 정리한다.
