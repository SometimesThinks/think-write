# Commit Deep Dive - 2026-03-20 - 7b34d6c

## Commit
- Full SHA: `7b34d6cecaba93321bc95e6070219f52dc3379d1`
- Short SHA: `7b34d6c`
- Subject: `chore: 프로젝트 초기 설정`
- Date: `2026-03-20`

## What changed
- Next.js App Router 기반 프로젝트를 루트에 세팅하고 `app/layout.tsx`, `app/page.tsx`, `app/globals.css`로 기본 앱 진입 구조를 만들었다.
- `package.json`에 Next.js 16, React 19, TypeScript, Tailwind CSS 4, ESLint, Prettier, 그리고 Tiptap 관련 패키지(`@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit`)를 추가했다.
- `app/page.tsx`를 기본 템플릿 대신 ThinkWrite MVP 방향을 보여주는 editor shell 화면으로 바꿨다.
- 화면 안에 학습 언어 설정, Ghost Complete 동작 힌트, BYOK provider 상태, 이후 구현 타깃을 모두 정적인 UI로 먼저 배치했다.
- `eslint.config.mjs`, `.prettierrc`, `.gitignore`, `tsconfig.json`을 추가해 초기 코드 규칙과 개발 환경을 정리했다.
- `src/server/lib`, `src/server/providers`, `src/server/services`, `src/server/use-cases` 디렉터리를 비워 둔 채 생성해 이후 서버 책임 분리를 준비했다.

## Why it matters
- 이 커밋은 단순한 “프로젝트 생성”이 아니라, ThinkWrite가 채팅형 도구가 아니라 편집기 중심 제품이라는 방향을 첫 화면에서 바로 드러낸다.
- 문서에 적힌 MVP 범위인 학습 언어 설정, Ghost Complete, BYOK provider 연동을 실제 화면 요소로 먼저 번역했기 때문에 이후 구현 우선순위를 흔들리지 않게 잡을 수 있다.
- Tiptap 의존성은 이미 추가했지만 실제 편집기 마운트는 아직 하지 않았기 때문에, “도구 도입”과 “기능 연결”을 분리해서 진행하는 초기 설계 흐름을 읽을 수 있다.
- 서버 폴더 경계를 먼저 만든 점은 나중에 provider 연동 로직이 커져도 Next 앱 내부에서 use-case / service / provider 책임을 분리하려는 의도를 보여준다.

## Key files
- `package.json`: 제품 MVP에 필요한 런타임 스택과 개발 도구 체인을 한 번에 어떻게 고정했는지 볼 수 있다.
- `app/page.tsx`: 기획 문서의 핵심 가치와 MVP 범위를 실제 editor shell UI로 옮기는 방식을 배울 수 있다.
- `app/layout.tsx`: App Router에서 전역 메타데이터와 루트 레이아웃을 어떻게 설정하는지 확인하기 좋다.
- `eslint.config.mjs`: Next.js flat config 위에 import 정렬, `eqeqeq` 같은 팀 규칙을 얹는 초기 품질 기준을 보여준다.
- `src/server/providers/.gitkeep`: 아직 구현 전이어도 provider 계층을 별도 폴더로 분리하려는 아키텍처 의도를 읽을 수 있다.

## Concepts to study
- Next.js App Router의 `layout.tsx` / `page.tsx` 역할 분리
- React Server Component와 Client Component 경계 설정
- Tiptap StarterKit, `@tiptap/react`, `@tiptap/pm`의 역할 차이
- Tailwind CSS 4와 Prettier plugin 조합 방식
- ESLint flat config와 `eslint-config-next` 확장 방법
- use-case / service / provider 레이어 분리 패턴
- Ghost text UI를 위한 ProseMirror decoration 개념

## Deep dive questions
- 왜 이 시점에서 실제 Tiptap 에디터 구현보다 editor shell 화면과 제품 방향을 먼저 고정하는 것이 유리할까?
- ThinkWrite처럼 편집기 중심 제품에서 어떤 부분은 server component로 두고, 어떤 부분부터 client component로 전환해야 할까?
- Tiptap 패키지를 먼저 설치해두는 접근의 장점과 단점은 무엇일까? 아직 사용하지 않는 의존성을 미리 두는 비용은 없을까?
- `src/server/use-cases`, `services`, `providers`를 Next 앱 안에 미리 나눠두면 이후 BYOK provider 연동에서 어떤 이점이 생길까?
- 현재 `app/page.tsx`처럼 정적인 shell을 먼저 만드는 방식은 제품 검증과 구현 속도 측면에서 어떤 trade-off가 있을까?

## Interview framing
- 문서에 적힌 MVP 범위를 바로 구현 세부 기능으로 뛰어들기보다, 먼저 editor shell UI로 번역해 제품 방향과 우선순위를 코드에 고정했다.
- 초기 세팅 단계에서 lint, format, typecheck, import 규칙까지 잡아 이후 기능 개발 중 품질 기준이 흔들리지 않도록 했다.
- Tiptap과 서버 계층 구조를 미리 준비하되 실제 복잡한 기능은 뒤로 미뤄, 도입 비용과 구현 범위를 분리하는 식으로 MVP 리스크를 낮췄다.

## Next study actions
### 15 min
- `app/layout.tsx`와 `app/page.tsx`를 기준으로 App Router 진입 구조를 손으로 다시 그려본다.
- `package.json`에서 Tiptap 관련 3개 패키지가 각각 왜 필요한지 한 줄씩 정리한다.

### 30 min
- 현재 `app/page.tsx`에서 나중에 client component로 분리될 가능성이 높은 영역을 표시해본다.
- `eslint.config.mjs`의 각 규칙이 실제 협업 또는 유지보수에 어떤 영향을 주는지 메모한다.

### 60 min
- Tiptap으로 최소 편집기를 붙일 때 필요한 컴포넌트 구조를 조사하고, 현재 shell에 어떻게 연결될지 별도 노트로 설계한다.
- ProseMirror decoration이 ghost suggestion UI에 어떻게 쓰일 수 있는지 예제 코드를 읽고 ThinkWrite 맥락으로 요약한다.
